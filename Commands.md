### Day 2 - Git init & Hidden Folders

```Bash 
git --version -> gives version of git
git status -> stores status of changes in files & sub-folders
git init -> intiailzed the repo(one time per project)

```